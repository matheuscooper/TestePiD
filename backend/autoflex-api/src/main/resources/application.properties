# Oracle
quarkus.datasource.db-kind=oracle
quarkus.datasource.username=app
quarkus.datasource.password=app
quarkus.datasource.jdbc.url=jdbc:oracle:thin:@//localhost:1521/FREEPDB1

# Hibernate (DEV)
quarkus.hibernate-orm.database.generation=drop-and-create
quarkus.hibernate-orm.log.sql=true

# HTTP
quarkus.http.port=8080

# # CORS - Configuração para quarkus-rest (Quarkus 3.x)
# quarkus.http.cors.enabled=true
# quarkus.http.cors.origins=http://localhost:3001,http://localhost:5173
# quarkus.http.cors.methods=GET,POST,PUT,DELETE,OPTIONS,PATCH
# quarkus.http.cors.headers=accept,authorization,content-type,x-requested-with
# quarkus.http.cors.access-control-allow-credentials=true

# # Dev mode - permitir todas as origens em desenvolvimento
# %dev.quarkus.http.cors.origins=*